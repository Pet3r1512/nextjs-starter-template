import ThemeToggle from "@/components/Buttons/ThemeToggle";

export default function Header() {
  return (
    <header className="max-w-7xl mx-auto py-5 px-5 lg:px-0">
      <ThemeToggle />
    </header>
  );
}
